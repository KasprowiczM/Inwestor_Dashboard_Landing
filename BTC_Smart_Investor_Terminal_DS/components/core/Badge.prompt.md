Status/data badge. Use `Badge` for data-freshness (live/stale/missing) and `StatusChip` for verdict capsules.

```jsx
<Badge tone="live">Live</Badge>
<Badge tone="stale" dot={false}>12m ago</Badge>
```
